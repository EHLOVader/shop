# ===================================
#  List Column Definitions
# ===================================

columns:

    created_at:
        label: Date
        type: partial
        path: @/plugins/bedard/shop/models/transaction/_created.htm

    # updated_at:
    #     label: Time Since
    #     type: timesince

    customer_name:
        label: Customer

    customer_email:
        label: Email

    amount:
        label: Amount
        select: (SELECT amount FROM bedard_shop_carts WHERE transaction_id = bedard_shop_transactions.id LIMIT 1)

    coupon:
        label: Coupon
        type: partial
        path: @/plugins/bedard/shop/models/transaction/_coupon.htm

    payment:
        label: Payment